# 宿舍安全风险识别系统 —— 验收测试文档

> **目标读者**：项目验收人员  
> **目标**：根据验收人员电脑配置，选择合适的运行方案，从零开始跑出分析结果  
> **预计耗时**：首次 30-50 分钟（含模型下载），后续 2-5 分钟

---

## 〇、第一步：确定你电脑的配置

在开始之前，先确认验收电脑的硬件。打开命令行（Win+R → 输入 `cmd`），执行：

```bash
wmic path win32_VideoController get name,adapterram
```

对照输出结果，确定方案：

| 你的 GPU / 显存 | 推荐方案 | 可用模型 | 预期效果 |
|------|------|------|------|
| 无独显 / 集成显卡 | **方案 C：CPU 运行** | 2B，CPU 推理 | 较慢但完整 |
| NVIDIA GTX/RTX，< 8 GB | **方案 B：本地 GPU** | 2B，GPU 推理 | 较快，JSON 偶有失败 |
| NVIDIA RTX，≥ 16 GB | **方案 B：本地 GPU** | 8B，GPU 推理 | 最佳效果 |
| # 以上都不满足 | **方案 A：云端** | 8B，5090 GPU | 最佳效果，~3 元 |

> **大多数验收电脑是集成显卡或入门独显，走方案 C 或方案 A 最靠谱。**
> 如果不确定，直接走**方案 A（云端）**，效果最好。

---

## 一、方案选择速查

| | 方案 A：云端 | 方案 B：本地 GPU | 方案 C：本地 CPU |
|------|-------------|-----------------|-----------------|
| 需要什么 | 网络 + 浏览器 | NVIDIA 独显 ≥ 6GB | 16GB RAM 以上 |
| 费用 | ~3 元 | 免费 | 免费 |
| 模型 | 8B（最强） | 2B 或 8B | 2B |
| 速度 | ~8s/张 | ~25s/张（2B） | ~150s/张 |
| 效果 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| 验收推荐度 | **首选** | 次选 | 备选 |

---

## 二、方案 A：云端运行（推荐，效果最好）

> 适合所有验收电脑，无需本地 GPU。

### 2.1 注册 & 租用 GPU

1. 打开 [https://www.autodl.com](https://www.autodl.com) → 注册 → 实名认证（支付宝/微信扫码）
2. 充值 **10 元**（实际只需 ~3 元）
3. 进入「算力市场」→ 选 **RTX 4090** 或 **RTX 5090**
4. 镜像选「PyTorch 2.x + CUDA 12.x」，数据盘 **50 GB**
5. 点击「立即创建」→ 等待开机

### 2.2 进入终端

点击 **「JupyterLab」** → 顶部菜单 File → New → Terminal

### 2.3 创建环境 + 下载模型

逐行复制执行：

```bash
# 创建虚拟环境
python -m venv ~/venv --system-site-packages
source ~/venv/bin/activate
pip install pillow -q
```

然后下载模型（约 10-20 分钟，17 GB）：

```bash
python
```

进入 Python 交互模式后，逐行输入：

```python
import os
os.environ["MODELSCOPE_CACHE"] = "/root/autodl-tmp/models"
from modelscope import snapshot_download
snapshot_download("Qwen/Qwen3-VL-8B-Instruct", cache_dir="/root/autodl-tmp/models")
```

等待看到 `>>>` 回到干净提示符，Ctrl+D 退出 Python。

### 2.4 上传项目文件

JupyterLab 左侧导航到 `/root/autodl-tmp/`，新建文件夹 `project`。

把以下文件拖拽上传到 `project/`：
- `risk_analyser.py`
- `dorm_photos/` 文件夹（含测试图片）

### 2.5 运行

```bash
source ~/venv/bin/activate
cd /root/autodl-tmp/project
MODEL_PATH=/root/autodl-tmp/models/models/Qwen--Qwen3-VL-8B-Instruct/snapshots/master
python risk_analyser.py -m $MODEL_PATH
```

约 2-3 分钟完成，看到 `All done!`。

### 2.6 查看 & 下载结果

JupyterLab 左侧双击 `risk_report.md` 查看报告，右键 → Download 下载。

### 2.7 关机

⚠️ **AutoDL 控制台 → 关机**（不关机持续扣费）

---

## 三、方案 B：本地 GPU 运行

> 适合：有 NVIDIA 显卡且 ≥ 6 GB 显存的电脑。  
> 先执行第〇步确认配置。

### B1：显存 ≥ 16 GB（RTX 4080/4090/5090 等）

可以跑 8B 模型。同方案 A 的模型下载和运行步骤，但全部在本地终端执行：

```bash
# 安装依赖
pip install torch transformers accelerate pillow modelscope -q

# 下载 8B 模型（约 17 GB，需要 80 GB 磁盘空间）
python -c "from modelscope import snapshot_download; snapshot_download('Qwen/Qwen3-VL-8B-Instruct', cache_dir='./models')"

# 准备图片到 dorm_photos/，然后运行
python risk_analyser.py -m ./models/Qwen/Qwen3-VL-8B-Instruct
```

### B2：显存 6-15 GB（RTX 2060/3060/4060 等）

只能跑 2B 模型。操作步骤：

```bash
# 1. 安装依赖
pip install torch transformers accelerate pillow modelscope -q

# 2. 下载 2B 模型（约 4 GB，5-10 分钟）
python -c "from modelscope import snapshot_download; snapshot_download('Qwen/Qwen3-VL-2B-Instruct', cache_dir='./models')"

# 3. 准备图片到 dorm_photos/

# 4. 运行（2B 模型不支持 load_in_4bit，先去掉）
sed -i 's/load_in_4bit=True,//' risk_analyser.py
python risk_analyser.py -m ./models/Qwen/Qwen3-VL-2B-Instruct
```

> **注意**：2B 模型推理能力有限，可能有部分图片 JSON 解析失败或输出重复。这是模型规模限制，非代码 bug。

---

## 四、方案 C：本地 CPU 运行（无需 GPU）

> 适合：没有独显，但内存 ≥ 16 GB 的电脑。  
> 大多数办公本、轻薄本走这个方案。

### 4.1 安装 CPU 版 PyTorch

```bash
pip install torch transformers accelerate pillow modelscope -q
```

### 4.2 下载 2B 模型

```bash
python -c "from modelscope import snapshot_download; snapshot_download('Qwen/Qwen3-VL-2B-Instruct', cache_dir='./models')"
```

约 5-10 分钟，4 GB。

### 4.3 运行

```bash
python risk_analyser.py -m ./models/Qwen/Qwen3-VL-2B-Instruct
```

> **CPU 推理较慢**：每张图片约 2-3 分钟。9 张图片预计 **20-30 分钟**完成。请耐心等待，不要中途关闭。

---

## 五、验证结果是否正常

### 5.1 Console 应看到

```
============================================================
  Dormitory Safety Risk Analyzer
  Model: Qwen3-VL-8B-Instruct
============================================================
Loading model: ...
Model loaded (GPU).

Found N images, starting analysis...

[1/N] xxx.jpg
  [OK] Risks: X  (High:X Medium:X Low:X)
...
Results saved: results.json
Report saved: risk_report.md
All done!
```

### 5.2 输出文件

- `results.json` —— 合法 JSON，可用记事本或浏览器打开
- `risk_report.md` —— Markdown 格式，推荐用 Typora/VS Code 打开，或拖到浏览器查看

### 5.3 验收通过标准

| 检查项 | 通过标准 |
|--------|---------|
| 程序运行 | 无报错，正常退出（`All done!`） |
| JSON 输出 | 所有图片均有结构化结果，error 率 ≤ 1 张 |
| 风险检出 | 至少部分图片检出了风险（8B）或至少产生了分析输出（2B） |
| 报告完整性 | 每张图有场景描述、风险详情（包含 type/level/reason/suggestion） |
| 报告可读性 | 中文正常显示，无明显乱码 |

> **2B 模型的特殊说明**：如果验收使用的是方案 B2 或方案 C（2B 模型），允许个别图片 JSON 解析失败（raw_output 中可见模型确实做了分析，只是格式不完全符合 JSON 规范）。这是 2B 模型的已知限制，在 `项目报告.md` 中有详细对比数据。

---

## 六、常见问题

| 现象 | 原因 | 解决 |
|------|------|------|
| `ModuleNotFoundError: No module named 'torch'` | 依赖未安装 | `pip install torch` |
| `CUDA out of memory` | 显存不够 | 换 2B 模型（方案 B2）|
| `load_in_4bit` 报错 | transformers 版本过新 | `sed -i 's/load_in_4bit=True,//' risk_analyser.py` |
| JSON 全部解析失败 | 2B 模型 + 复杂 Prompt | 这是 2B 已知局限，切换到方案 A（8B 云端）可解决 |
| 下载速度很慢 | 网络问题 | 正常，ModelScope 国内一般 5-15 MB/s |
| 中文显示乱码 | 终端编码问题 | 不影响结果文件（UTF-8），用浏览器打开 risk_report.md 即可 |

---

## 七、测试图片说明

项目 `dorm_photos/` 已包含测试图片，可直接使用。如需替换为自己的测试图片，支持 `.jpg` `.jpeg` `.png` `.bmp` `.gif` `.webp` 格式，放入 `dorm_photos/` 即可。

建议测试图片覆盖以下场景以全面验证系统能力：

| 场景 | 预期 |
|------|------|
| 整洁的宿舍/卧室 | 0 风险或少量 Low |
| 桌面上插线板靠近水杯 | 触电风险 |
| 过道有杂物/箱子堆积 | 通道阻塞 |
| 桌面杂乱、垃圾堆积 | 清洁度异常 |

---

## 附录：文件清单

验收时请确保以下文件齐全：

```
□ risk_analyser.py          # 主程序
□ dorm_photos/              # 测试图片（至少 5 张）
□ 项目报告.md               # 论文/报告
□ 技术文档.md               # 技术说明
□ 测试文档.md               # 本文档
□ Phase1_风险定义与Prompt设计.md   # 参考
□ Phase2_当前Prompt设计文档.md    # 参考
```
